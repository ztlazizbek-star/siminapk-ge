# Cafe Simin

Современное приложение для заказа еды в кафе, построенное на React и Next.js с интеграцией MySQL базы данных.

## Особенности

- 🍔 Интерактивное меню с категориями
- 📱 Адаптивный дизайн для мобильных устройств
- 🛒 Корзина покупок с локальным хранением
- 👤 Профиль пользователя
- 📖 Instagram-стиль историй
- 🔔 Уведомления о действиях
- 🗄️ Интеграция с MySQL базой данных
- 🔧 PHP API для управления данными

## Технологии

- **Frontend**: React 19, Next.js 14, TypeScript
- **Backend**: PHP 8.0+, MySQL 8.0+
- **Стили**: Tailwind CSS v4, Radix UI
- **Иконки**: SVG иконки, Lucide React
- **Анимации**: Tailwind CSS Animate

## Установка

### 1. Клонируйте репозиторий:
\`\`\`bash
git clone <repository-url>
cd zireh
\`\`\`

### 2. Установите зависимости:
\`\`\`bash
npm install
\`\`\`

### 3. Настройка базы данных:

#### Создайте базу данных MySQL:
\`\`\`sql
CREATE DATABASE cg47924_simin;
\`\`\`

#### Импортируйте структуру таблиц:
\`\`\`sql
-- Таблица администраторов
CREATE TABLE `admins` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `username` varchar(50) NOT NULL,
  `password` varchar(255) NOT NULL,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- Таблица товаров меню
CREATE TABLE `menu_items` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `name` varchar(255) NOT NULL,
  `top_category` varchar(50) NOT NULL,
  `subcategory` varchar(50) NOT NULL,
  `description` text,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

-- Таблица вариантов товаров
CREATE TABLE `item_variants` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `item_id` int(11) NOT NULL,
  `volume` varchar(10) NOT NULL,
  `price` decimal(10,2) NOT NULL,
  `image` varchar(255) NOT NULL,
  PRIMARY KEY (`id`),
  KEY `item_id` (`item_id`),
  CONSTRAINT `item_variants_ibfk_1` FOREIGN KEY (`item_id`) REFERENCES `menu_items` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
\`\`\`

### 4. Настройка PHP API:

Убедитесь, что в файле `api/config.php` указаны правильные данные для подключения к базе данных:

\`\`\`php
'db_host' => 'localhost',
'db_name' => 'cg47924_simin',
'db_user' => 'cg47924_simin',
'db_pass' => '123456789'
\`\`\`

### 5. Запустите проект:

#### Для разработки:
\`\`\`bash
npm run dev
\`\`\`

#### Для продакшена:
\`\`\`bash
npm run build
npm run start
\`\`\`

### 6. Настройка веб-сервера:

Убедитесь, что ваш веб-сервер (Apache/Nginx) настроен для обработки PHP файлов и имеет доступ к папке `api/`.

## API Endpoints

- `GET /api/products.php` - Получить все товары с вариантами
- `GET /api/categories.php?type=main` - Получить основные категории
- `GET /api/categories.php?type=sub` - Получить подкатегории
- `GET /api/variants.php?item_id={id}` - Получить варианты товара

## Скрипты

- `npm run dev` - Запуск в режиме разработки
- `npm run build` - Сборка для продакшена
- `npm run start` - Запуск продакшен сервера
- `npm run lint` - Проверка кода линтером

## Структура проекта

\`\`\`
zireh/
├── app/                    # Next.js App Router
│   ├── globals.css        # Глобальные стили (консолидированы)
│   ├── layout.tsx         # Корневой layout
│   └── page.tsx           # Главная страница
├── components/            # React компоненты
│   ├── Header.tsx         # Шапка приложения
│   ├── Stories.tsx        # Instagram-стиль историй
│   ├── Categories.tsx     # Категории меню
│   ├── MenuItems.tsx      # Элементы меню
│   ├── Cart.tsx           # Корзина покупок (исправлена иконка)
│   └── ...               # Другие компоненты
├── api/                   # PHP API
│   ├── config.php         # Конфигурация базы данных
│   ├── products.php       # API для товаров
│   ├── categories.php     # API для категорий
│   └── variants.php       # API для вариантов товаров
├── public/               # Статические файлы
└── lib/                  # Утилиты
\`\`\`

## Требования к серверу

- **PHP**: 8.0 или выше
- **MySQL**: 8.0 или выше
- **Node.js**: 18.0 или выше
- **Веб-сервер**: Apache или Nginx с поддержкой PHP

## Развертывание

### Для статического хостинга (только фронтенд):
1. Запустите `npm run build`
2. Загрузите содержимое папки `out/` на хостинг

### Для полного развертывания с PHP:
1. Загрузите все файлы на хостинг с поддержкой PHP
2. Настройте базу данных MySQL
3. Обновите настройки в `api/config.php`
4. Убедитесь, что папка `api/` доступна через веб

## Исправления для VS Code Build

### ✅ Исправленные проблемы:
- **CSS конфликты**: Консолидированы все стили в `app/globals.css`
- **Font Awesome зависимость**: Удалена CDN ссылка, заменена на SVG иконки
- **CSS переменные**: Исправлен синтаксис `hsl(var(--variable))` на `oklch(var(--variable))`
- **Импорты CSS**: Исправлен конфликтующий импорт `../styles/globals.css`
- **TypeScript конфигурация**: Проверена и оптимизирована
- **Иконка корзины**: Заменена на встроенную SVG иконку для лучшей совместимости

### 🔧 Команды для сборки:
\`\`\`bash
# Проверка типов TypeScript
npx tsc --noEmit

# Сборка проекта
npm run build

# Запуск линтера
npm run lint
\`\`\`

### 📝 Примечания для разработки:
- Все стили теперь в одном файле `app/globals.css`
- Удалена папка `styles/` для избежания конфликтов
- Используются только встроенные иконки и SVG
- Tailwind CSS v4 с правильной конфигурацией

## Лицензия

MIT License
